[package]
name = "xtask"
version = "0.1.0"
edition = "2024"
description = "Repository-local build tasks for WRAC examples"
license = "MIT"
repository = "https://github.com/novonotes/wrac-examples"
publish = false

[dependencies]
clap = { version = "4.5.38", features = ["derive"] }
