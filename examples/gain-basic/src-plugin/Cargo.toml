[package]
name = "wrac_gain_basic_plugin"
version = "0.1.0"
edition = "2024"
description = "Basic gain plugin example for the WRAC stack"
license = "MIT"
repository = "https://github.com/novonotes/wrac-examples"
publish = false
build = "build.rs"

# WRAC metadata is the single source of truth for descriptors, GUI metadata,
# bundle names, wrapper arguments, AUv2 registration, WebView data dirs, and logs.
[package.metadata.wrac]
# Reverse-DNS identifier. Use your own domain or another namespace you control.
plugin_id = "com.yourcompany.examples.wrac-gain-basic"
# User-visible plugin name shown by hosts and the example GUI.
plugin_name = "WRAC Gain Basic"
# User-visible vendor name shown by hosts and the example GUI.
company_name = "Your Company"
# AUv2 component type. Audio effects usually use "aufx"; instruments usually use "aumu".
auv2_type = "aufx"
# AUv2 subtype. Pick a unique 4-byte ASCII code for each plugin from the same vendor.
auv2_subtype = "WgBn"
# AUv2 manufacturer code. Use the same 4-byte ASCII code across your plugin catalog.
auv2_manufacturer_code = "YCmp"
# User-visible standalone app name.
standalone_name = "WRAC Gain Basic Standalone"

[lib]
crate-type = ["rlib", "staticlib", "cdylib"]

[dependencies]
assert_no_alloc = { git = "https://github.com/Windfisch/rust-assert-no-alloc.git", rev = "d31f2d5f550ce339d1c2f0c1ab7da951224b20df", features = ["backtrace"] }
atomic_float = "1.1.0"
directories = "5"
log = "0.4"
novonotes_run_loop = { workspace = true }
parking_lot = "0.12"
serde = { version = "1", features = ["derive"] }
serde_json = "1.0"
time = { version = "0.3", features = ["formatting", "local-offset", "macros"] }
run_loop_timer = { workspace = true }
wrac_clap_adapter = { workspace = true }
wrac_wxp_gui = { workspace = true }
wxp = { workspace = true }

[lints.rust]
unreachable_pub = "deny"

[build-dependencies]
zip = { version = "2.4.2", default-features = false, features = ["deflate"] }
