# WRAC Gain Basic

> English version: [README.md](README.md)

WRAC plugin template を元にした、最小構成の audio effect example です。

```sh
cargo xtask build --plugin=gain-basic --target=clap
```

gain parameter、bypass parameter、state save/restore、template 由来の WebView
GUI を備えています。
