[package]
name = "wrac_sine_synth_plugin"
version = "0.1.0"
edition = "2024"
description = "Sine wave MIDI synth example for the WRAC stack"
license = "MIT"
repository = "https://github.com/novonotes/wrac-examples"
publish = false
build = "build.rs"

# Plugin identity lives here so descriptors, GUI metadata, bundle names,
# wrapper arguments, AUv2 codes, WebView data dirs, and log names all move
# together when this template is renamed.
[package.metadata.wrac]
plugin_id = "com.yourcompany.examples.wrac-sine-synth"
plugin_name = "WRAC Sine Synth"
company_name = "Your Company"
# AUv2 codes must be exactly 4 ASCII bytes. Instruments usually use "aumu".
auv2_type = "aumu"
auv2_subtype = "WSSn"
auv2_manufacturer_code = "YCmp"
standalone_name = "WRAC Sine Synth Standalone"

[lib]
crate-type = ["rlib", "staticlib", "cdylib"]

[dependencies]
assert_no_alloc = { git = "https://github.com/Windfisch/rust-assert-no-alloc.git", rev = "d31f2d5f550ce339d1c2f0c1ab7da951224b20df", features = ["backtrace"] }
atomic_float = "1.1.0"
directories = "5"
log = "0.4"
novonotes_run_loop = { workspace = true }
parking_lot = "0.12"
serde = { version = "1", features = ["derive"] }
serde_json = "1.0"
time = { version = "0.3", features = ["formatting", "local-offset", "macros"] }
run_loop_timer = { workspace = true }
wrac_clap_adapter = { workspace = true }
wrac_wxp_gui = { workspace = true }
wxp = { workspace = true }

[lints.rust]
unreachable_pub = "deny"

[build-dependencies]
zip = { version = "2.4.2", default-features = false, features = ["deflate"] }
